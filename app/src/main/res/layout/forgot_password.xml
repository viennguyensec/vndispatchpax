<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent" android:layout_height="match_parent">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:focusable="true"
        android:focusableInTouchMode="true">
        <Button
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/icon_chevron_left"
            android:id="@+id/btnBackToSignIn"
            android:layout_marginTop="25dp"
            android:textSize="17dp"
            android:background="@color/colorTransparent"
            android:layout_alignParentTop="true"
            android:layout_alignStart="@+id/textView4"
            android:textColor="#fff"
            android:layout_marginLeft="-10dp"/>

        <TextView
            style="@style/TextView"
            android:text="No worries. Simply the e-mail you used to register and we will send you an e-mail with a new password."
            android:id="@+id/textView4"
            android:layout_centerVertical="true"
            android:layout_alignParentTop="true"
            android:layout_marginTop="98dp"
            android:layout_marginBottom="10dp"/>
        <EditText
            style="@style/InputDefault"
            android:id="@+id/emailForgotPassword"
            android:inputType="textEmailAddress"
            android:layout_below="@+id/textView4"
            android:hint="Email"/>
        <Button
            style="@style/MainButton"
            android:id="@+id/sendEmailForgotPassword"
            android:text="SEND"
            android:layout_below="@+id/emailForgotPassword"/>


    </RelativeLayout>

</ScrollView>